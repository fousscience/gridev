/**
 * Seed Sanity (à exécuter une fois le projet Sanity créé) :
 *
 * 1. Créer un projet sur https://www.sanity.io/manage
 * 2. Renseigner NEXT_PUBLIC_SANITY_PROJECT_ID dans .env.local
 * 3. Créer un token avec droits d'écriture
 * 4. npx sanity dataset import sanity/seed.ndjson production --replace
 *
 * En attendant, le site sert le contenu de src/lib/fallback.ts
 */

export {};
